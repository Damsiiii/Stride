# MongoDB Atlas connection string
# Replace <user>, <pass>, and cluster URL with your own values
MONGODB_URI=mongodb+srv://<user>:<pass>@cluster.xxxxx.mongodb.net/stride?retryWrites=true&w=majority

# Port the Express server listens on
PORT=5000
